{
  "name": "shibui-ecosystem",
  "private": true,
  "description": "Monorepo para testing de componentes Shibui UI",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:react": "npm run dev -w apps/app-react",
    "start:svelte": "npm run dev -w apps/app-svelte",
    "start:angular": "npm run start -w apps/app-angular",
    "dev:all": "concurrently -n \"REACT,SVELTE,ANGULAR\" -c \"cyan,magenta,blue\" \"npm run start:react\" \"npm run start:svelte\" \"npm run start:angular\"",
    "build": "npm run build -w @shibui/ui",
    "prepare": "husky",
    "lint-staged": "lint-staged",
    "type-check": "npx tsc --noEmit --project packages/shibui-ui/tsconfig.json",
    "lint": "eslint . --config .config/.eslintrc.json --ignore-path .config/.eslintignore",
    "format": "prettier --write \"src/**/*.ts\" --config .config/.prettierrc.json"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "typescript": "^5.9.3"
  },
"lint-staged": {
  "**/*.{ts,html.ts}": [
    "eslint --config .config/.eslintrc.json --fix --max-warnings=0"
  ],
  "**/*.css": [
    "stylelint --config .config/.stylelintrc.json --fix",
    "prettier --write"
  ]
}
}
