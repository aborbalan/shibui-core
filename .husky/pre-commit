. "$(dirname -- "$0")/_/husky.sh"

# Type check en el workspace de la librería
npm run type-check -w @shibui/ui

# lint-staged ejecutado desde dentro del workspace para que los patrones src/** resuelvan correctamente
cd packages/shibui-ui && npx lint-staged
