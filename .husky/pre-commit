#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

FAILED=0

npm run type-check -w @shibui/ui || FAILED=1

cd packages/shibui-ui

npx lint-staged || FAILED=1

npx stylelint --config .config/.stylelintrc.json --fix "src/**/*.css" || FAILED=1

exit $FAILED