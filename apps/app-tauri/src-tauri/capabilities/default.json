{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities para las ventanas del macro entorno (main + secondary)",
  "windows": ["main", "secondary"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:webview:allow-create-webview-window"
  ]
}
