/** Tipos LOCALES de lib-panel. */

/** Tratamiento visual de la superficie del panel. */
export type LibPanelVariant = 'surface' | 'elevated' | 'outline';

/** Escala de padding interno del panel. */
export type LibPanelPadding = 'none' | 'sm' | 'md' | 'lg';
