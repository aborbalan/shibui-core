// warp — deformación en remolino (uzumaki). Adaptador opcional, puro y testeable en node.
// Ω es estética + ofuscación, nunca la clave.
export {
  warp,
  warpAll,
  unwarpAll,
  spiral,
  type UzumakiOptions,
  type SpiralOptions,
} from './uzumaki';
